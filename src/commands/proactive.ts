import type { Command } from '../commands.js'
const cmd = { type: 'local-jsx', name: '__placeholder__', description: 'Not available in this build', load: async () => null } satisfies Command
export default cmd
